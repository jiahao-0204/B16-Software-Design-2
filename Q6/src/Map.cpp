#include "Map.hpp"

void Map::addNode(Node & new_node) {
    node_list.push_back(& new_node);
};

bool Map::check_connectivity(std::string location_A, std::string location_B) {
    // todo place holder code
    return true;
};